# 斗地主端到端模型评测对比报告

本文对 `outputs/doudizhu_model_eval` 下的斗地主模型评测结果进行系统分析。核心目标不是简单挑最高胜率，而是解释模型能力从“无法稳定 GUI 出牌”，到“会执行目标动作”，再到“能端到端打完整局并减少环境 fallback”，最后到“强化学习后期出现质量回落”的完整演进链条。

需要先明确一个重要口径：除 `kimi_k26_raw` 外，其余模型结果来自 `scripts/eval_doudizhu_model.py` 的在线环境评测；`kimi_k26_raw` 来自 `data_synthesis/doudizhu_end_to_end_sft/raw` 的离线原始合成轨迹统计。Kimi K2.6 因此更适合作为“教师数据源 / synthetic raw behavior reference”，而不是和本地 checkpoint 完全同协议、同 seed 的 leaderboard 项。尽管如此，它非常适合放进核心对比，因为它回答了一个关键问题：训练数据源本身大概处在什么能力水平，以及后续 SFT + GRPO 是否真正超过了教师行为。

## 一、七个核心结果总览

建议展示的七个核心结果如下：

| 角色 | 目录 | 评测单位 | 胜场 | 核心定位 |
|---|---|---:|---:|---|
| Qwen3.5-4B baseline | `qwen35_4b` | 128 局在线轨迹 | 0 / 128 | 主训练底座，展示原始 4B 模型几乎不会端到端玩。 |
| Qwen3.5-9B baseline | `qwen35_9b` | 128 局在线轨迹 | 0 / 128 | 大模型底座对照，说明参数变大不能自然解决 GUI 决策。 |
| Kimi K2.6 raw | `kimi_k26_raw` | 485 局离线 raw 轨迹 | 51 / 485 | 教师数据源参考，有明显能力，但仍有噪声和 fallback。 |
| Grounding-only SFT 400 | `qwen35_4b_doudizhu_grounding_SFT_400_steps` | 256 局在线轨迹 | 0 / 256 | 会执行指定动作，但不能端到端赢局。 |
| End-to-end mix SFT 600 | `qwen35_4b_doudizhu_grounding_qa_end_to_end_mix_SFT_600_steps` | 256 局在线轨迹 | 12 / 256 | SFT 阶段最强端到端模型。 |
| GRPO step 60 | `grpo_qwen3_5_4b_doudizhu_zh_with_SFT_global_step_60` | 256 局在线轨迹 | 45 / 256 | 综合最强模型，主结果。 |
| GRPO step 105 | `grpo_qwen3_5_4b_doudizhu_zh_with_SFT_global_step_105` | 256 局在线轨迹 | 42 / 256 | 后期 checkpoint，展示强化学习继续训练后的质量回落。 |

完整游戏的关键指标如下：

| 模型 / 阶段 | `won` | `total_reward` | `projection_valid_rate` | `rule_action_valid_rate` | `fallback_rate` | `model_hand_depletion_rate` | `legal_given_projection_rate` |
|---|---:|---:|---:|---:|---:|---:|---:|
| Qwen3.5-4B baseline | 0.0000 | -0.9398 | 0.6695 | 0.0528 | 0.9472 | 0.0121 | 0.0784 |
| Qwen3.5-9B baseline | 0.0000 | -0.9448 | 0.5949 | 0.0285 | 0.9715 | 0.0125 | 0.0486 |
| Kimi K2.6 raw | 0.1052 | -0.5383 | 0.9187 | 0.6332 | 0.3665 | 0.4891 | 0.6876 |
| Grounding-only SFT 400 | 0.0000 | -0.8680 | 0.9381 | 0.2276 | 0.7724 | 0.0854 | 0.2423 |
| End-to-end mix SFT 600 | 0.0469 | -0.6602 | 0.9485 | 0.6830 | 0.3170 | 0.4223 | 0.7202 |
| GRPO step 60 | 0.1758 | -0.3305 | 0.9921 | 0.8042 | 0.1958 | 0.6977 | 0.8104 |
| GRPO step 105 | 0.1641 | -0.3990 | 0.9325 | 0.6804 | 0.3196 | 0.5619 | 0.7275 |

对应的 grounding / GUI 执行能力如下。Kimi K2.6 raw 没有同协议 grounding 评测，因此这里标为 N/A：

| 模型 / 阶段 | grounding `target_action_match` | nonpass target match | grounding `projection_valid` | grounding `click_valid_ratio` |
|---|---:|---:|---:|---:|
| Qwen3.5-4B baseline | 0.1204 | 0.0122 | 0.7316 | 0.3750 |
| Qwen3.5-9B baseline | 0.1501 | 0.0331 | 0.8781 | 0.4879 |
| Kimi K2.6 raw | N/A | N/A | N/A | N/A |
| Grounding-only SFT 400 | 0.9309 | 0.9090 | 1.0000 | 0.9874 |
| End-to-end mix SFT 600 | 0.9515 | 0.9362 | 1.0000 | 0.9911 |
| GRPO step 60 | 0.8784 | 0.8400 | 1.0000 | 0.9782 |
| GRPO step 105 | 0.8381 | 0.7869 | 1.0000 | 0.9672 |

这些指标应一起读，而不是只看胜率。

`projection_valid_rate` 衡量模型输出是否能被投影器解析成 GUI 动作。`rule_action_valid_rate` 更严格，表示执行后是否形成规则合法动作。两者之间的差距很关键：baseline 的输出有时能被解析，但真正落到环境里几乎全是非法或无效操作。4B baseline 的 `projection_valid_rate=0.6695`，但 `rule_action_valid_rate=0.0528`，说明“格式看起来像动作”和“真正能在斗地主里出合法牌”之间存在巨大鸿沟。GRPO step 60 把这两个指标分别推到 `0.9921` 和 `0.8042`，说明模型不只是学会了 XML / tool call 格式，而是学会了把视觉状态、手牌选择、按钮点击和斗地主规则对齐起来。

`fallback_rate` 和 `model_hand_depletion_rate` 是展示端到端能力时非常有说服力的指标。环境 fallback 越高，说明真实推进更多依赖环境兜底，而不是模型自己的动作。4B baseline 的 `fallback_rate=0.9472`，几乎每一步都靠 fallback；`model_hand_depletion_rate=0.0121`，意味着模型自己真正减少手牌的贡献近乎为零。End-to-end mix SFT 600 把 fallback 降到 `0.3170`，GRPO step 60 进一步降到 `0.1958`；同时模型自主出牌推进能力从 baseline 的 `0.0121` 提升到 `0.6977`。这条线比单独的胜率更能说明模型确实在“自己打牌”。

## 二、核心结果深挖

### 1. Baseline：原始 VLM 并不会自然学会 GUI 斗地主

Qwen3.5-4B 和 Qwen3.5-9B 都是很重要的负对照。两者完整游戏胜率均为 0，而且 9B 并没有比 4B 更好：

| 模型 | 胜率 | 奖励 | 规则合法动作率 | fallback | fallback pass |
|---|---:|---:|---:|---:|---:|
| Qwen3.5-4B | 0.0000 | -0.9398 | 0.0528 | 0.9472 | 0.8867 |
| Qwen3.5-9B | 0.0000 | -0.9448 | 0.0285 | 0.9715 | 0.9126 |

这说明任务难点不是“语言模型是否知道斗地主规则”这么简单，而是多种能力的组合：看图识别手牌、维护当前轮次、选择符合牌型和大小关系的动作、把语义动作映射到正确坐标、最后点击提交按钮。9B 在 grounding 单步评测里略好于 4B，但完整游戏反而更差，说明更大的基础模型如果没有任务内监督和环境反馈，并不能自动形成稳定 GUI policy。

从 `fallback_pass_rate` 也能看到 baseline 的失败形态：4B 为 `0.8867`，9B 为 `0.9126`。大量 fallback 最终变成 pass，模型不是在积极推进手牌，而是在持续失去主动权。这个现象和 `model_hand_depletion_rate` 接近 0 一起，构成了很清晰的 baseline 图景：原始模型可以输出一些表面格式，但几乎不能形成可执行、可推进的斗地主行为。

### 2. Kimi K2.6 raw：教师数据有真实能力，但不是完美策略

Kimi K2.6 raw 的离线统计为：胜率 `0.1052`，奖励 `-0.5383`，规则合法动作率 `0.6332`，fallback `0.3665`，模型自主出牌推进 `0.4891`。相比 Qwen baseline，这已经是质的提升。它证明用于合成端到端数据的教师不是随机行为：它能够在相当比例的状态下给出可解析、可点击、可通过规则检验的动作，也能赢下一部分完整牌局。

但 Kimi raw 同时暴露出教师数据的噪声上限。`fallback_rate=0.3665` 说明超过三分之一的步骤仍需环境兜底；`legal_given_projection_rate=0.6876` 说明即便输出能被解析，也仍有相当一部分动作无法成为规则合法动作。换句话说，Kimi K2.6 是一个有策略能力的教师，但不是干净的 oracle。

这一点对后续结果解释非常关键：如果学生模型经过 SFT + GRPO 后只接近 Kimi raw，那只能说明学生复现了教师；但 GRPO step 60 的完整游戏胜率 `0.1758`、奖励 `-0.3305`、规则合法动作率 `0.8042`、fallback `0.1958` 都优于 Kimi raw。虽然 Kimi raw 不是同协议在线评测，不能作为严格 leaderboard，但它仍然支持一个强叙事：训练流程不只是蒸馏教师文本，而是通过端到端环境反馈把策略质量推到了教师 raw 轨迹之上。

### 3. Grounding-only SFT：动作执行能力很强，但不是完整打牌能力

Grounding-only SFT 400 是一个很重要的中间阶段。它在 grounding 评测上非常强：

| 模型 | `target_action_match` | `nonpass_target_action_match` | `projection_valid` | `click_valid_ratio` |
|---|---:|---:|---:|---:|
| Qwen3.5-4B baseline | 0.1204 | 0.0122 | 0.7316 | 0.3750 |
| Qwen3.5-9B baseline | 0.1501 | 0.0331 | 0.8781 | 0.4879 |
| Grounding-only SFT 400 | 0.9309 | 0.9090 | 1.0000 | 0.9874 |

这说明 grounding-only SFT 已经基本解决了“给定目标动作，如何在图上点出来”的问题。它能稳定生成可解析动作，能正确点击，能匹配非 pass 目标牌型。

但是它完整游戏胜率仍是 0，`rule_action_valid_rate` 只有 `0.2276`，`fallback_rate` 高达 `0.7724`。原因也很直接：grounding 任务给了模型目标动作，它训练的是执行器；完整游戏中目标动作必须由模型自己根据局面生成。Grounding-only SFT 学会了“怎么点”，但还没学会“该出什么”。这组结果很好地证明了任务分解的必要性：GUI grounding 是必要能力，但不足以形成完整游戏策略。

### 4. End-to-end mix SFT：从执行器过渡到策略模型

End-to-end mix SFT 600 是 SFT 阶段最适合展示的 checkpoint。与 grounding-only SFT 400 相比，它完整游戏胜率从 0 提升到 `0.0469`，奖励从 `-0.8680` 提升到 `-0.6602`，规则合法动作率从 `0.2276` 提升到 `0.6830`，fallback 从 `0.7724` 降到 `0.3170`。

这说明端到端数据补上了 grounding-only 缺失的关键环节：模型开始能自己判断局势并选择动作，而不只是执行别人给出的目标动作。更重要的是，end-to-end mix SFT 没有牺牲 grounding 能力，反而在 grounding state 指标上更强：

| 模型 | grounding `target_action_match` | nonpass match | 完整游戏胜率 | 规则合法动作率 |
|---|---:|---:|---:|---:|
| Grounding-only SFT 400 | 0.9309 | 0.9090 | 0.0000 | 0.2276 |
| End-to-end mix SFT 600 | 0.9515 | 0.9362 | 0.0469 | 0.6830 |

这组对比非常适合展示你的 SFT 数据设计：混入端到端轨迹不是简单让模型“背答案”，而是让模型同时保持点击执行能力和局面决策能力。它把模型从 GUI executor 推向 game-playing agent。

### 5. GRPO step 60：最佳综合结果

GRPO step 60 是当前最强综合 checkpoint。相对 end-to-end mix SFT 600，它的完整游戏胜率从 `0.0469` 提升到 `0.1758`，约为 3.75 倍；总奖励从 `-0.6602` 提升到 `-0.3305`；规则合法动作率从 `0.6830` 提升到 `0.8042`；fallback 从 `0.3170` 降到 `0.1958`；模型自主出牌推进从 `0.4223` 提升到 `0.6977`。

| 对比 | 胜率 | 奖励 | 规则合法动作率 | fallback | 自主出牌推进 |
|---|---:|---:|---:|---:|---:|
| End-to-end mix SFT 600 | 0.0469 | -0.6602 | 0.6830 | 0.3170 | 0.4223 |
| GRPO step 60 | 0.1758 | -0.3305 | 0.8042 | 0.1958 | 0.6977 |
| 变化 | +0.1289 | +0.3297 | +0.1211 | -0.1211 | +0.2754 |

这里的改进不是只体现在胜率上，而是多个行为质量指标同时改善。`model_hand_depletion_rate` 的提升尤其重要，因为它表示模型自己真正把手牌打出去，而不是靠 fallback 或 pass 混过回合。GRPO step 60 也保留了很高的输出格式和点击质量：`projection_valid_rate=0.9921`，`click_valid_ratio=0.9676`。

和 Kimi raw 相比，GRPO step 60 也有很强的展示价值：

| 模型 | 胜率 | 奖励 | 规则合法动作率 | fallback | 自主出牌推进 |
|---|---:|---:|---:|---:|---:|
| Kimi K2.6 raw | 0.1052 | -0.5383 | 0.6332 | 0.3665 | 0.4891 |
| GRPO step 60 | 0.1758 | -0.3305 | 0.8042 | 0.1958 | 0.6977 |

在严格表述上，应说明 Kimi 是离线 raw 统计，不是同 seed 在线评测；但作为数据源参考，它仍然显示出一个很有力的趋势：学生模型经由 SFT 和 GRPO 后，在在线环境中的综合指标超过了教师 raw 行为统计。

### 6. GRPO step 105：后期质量回落，而不是单纯“训练越久越好”

把 GRPO step 105 纳入核心展示，比选择 step 90 更有价值。step 90 只比 step 60 多 1 局胜场，叙事说服力弱；step 105 则在多个质量指标上同时低于 step 60，能更清楚地说明强化学习后期存在质量退化。

| 指标 | GRPO step 60 | GRPO step 105 | 解读 |
|---|---:|---:|---|
| `won` | 0.1758 | 0.1641 | 胜率略降，置信区间重叠，不宜单独强调显著下降。 |
| `total_reward` | -0.3305 | -0.3990 | 奖励明显变差。 |
| `projection_valid_rate` | 0.9921 | 0.9325 | 输出可投影性下降。 |
| `rule_action_valid_rate` | 0.8042 | 0.6804 | 合法动作率大幅下降。 |
| `fallback_rate` | 0.1958 | 0.3196 | 对环境兜底依赖升高。 |
| `model_hand_depletion_rate` | 0.6977 | 0.5619 | 自主打出手牌能力下降。 |
| grounding `target_action_match` | 0.8784 | 0.8381 | grounding 能力也有回落。 |

需要注意，胜率本身的 95% bootstrap CI 有重叠：step 60 为 `[0.1289, 0.2227]`，step 105 为 `[0.1211, 0.2070]`。因此报告里不应声称“胜率显著下降”。更准确的结论是：后期 checkpoint 在胜率上没有继续带来可靠收益，同时动作合法性、fallback、自主出牌推进和 grounding 指标均出现清晰回落。尤其是 `rule_action_valid_rate` 的 CI 从 step 60 的 `[0.7904, 0.8179]` 下降到 step 105 的 `[0.6610, 0.6991]`，`fallback_rate` 的 CI 从 `[0.1821, 0.2108]` 上升到 `[0.3001, 0.3401]`，这两个关键行为指标的区间不重叠，说明退化不是单个样本波动造成的表面现象。这说明 RL 后期综合行为质量发生退化，可能存在策略漂移、过优化局部奖励或格式 / 点击稳定性下降的问题。

## 三、从 baseline 到 GRPO 的能力跃迁

把七个核心点串起来，可以形成一条清晰的工作叙事：

1. 原始 Qwen3.5-4B / 9B 都无法端到端完成 GUI 斗地主。问题不是参数量不足，而是缺少任务内监督和环境反馈。
2. Kimi K2.6 raw 提供了有真实策略能力的合成数据源，但数据不是完美 oracle，仍有较高 fallback 和非法动作。
3. Grounding-only SFT 大幅提升动作执行和点击能力，但无法解决完整游戏中的自主决策。
4. End-to-end mix SFT 把模型从“执行目标动作”推进到“自己选择动作并打完整局”，完整游戏指标明显提升。
5. GRPO 在 SFT 基础上进一步优化环境回报，显著提高胜率、奖励、合法动作率和自主出牌推进。
6. RL 并非单调收益。step 105 的综合退化显示，需要用验证集或多指标 early stopping，而不能只按训练步数继续推进。

这条链条比单一胜率表更能展示工作价值，因为它把数据合成、监督微调、grounding、端到端决策和强化学习的贡献拆开了。

## 四、小 checkpoint 演进分析

### 4.1 Grounding-only SFT：400 步是代表点，继续训练收益有限

| checkpoint | 胜率 | 奖励 | 规则合法动作率 | fallback | 自主出牌推进 | grounding target match |
|---|---:|---:|---:|---:|---:|---:|
| 200 | 0.0000 | -0.8762 | 0.2062 | 0.7938 | 0.0617 | 0.8745 |
| 400 | 0.0000 | -0.8680 | 0.2276 | 0.7724 | 0.0854 | 0.9309 |
| 600 | 0.0000 | -0.8702 | 0.2235 | 0.7765 | 0.0754 | 0.9149 |

Grounding-only SFT 在 400 步达到较好状态：grounding target match 最高，完整游戏指标也略优。600 步没有进一步改善，反而略回落。因此选 400 步作为 grounding-only 代表是合理的。

更重要的是，三组 grounding-only checkpoint 胜率全部为 0。这再次说明 grounding 不是完整策略，单靠“给定动作后点对牌”不能解决斗地主的主动决策。

### 4.2 QA mix SFT：有一定格式和动作收益，但端到端策略不足

| checkpoint | 胜率 | 奖励 | 规则合法动作率 | fallback | 自主出牌推进 | grounding target match |
|---|---:|---:|---:|---:|---:|---:|
| 300 | 0.0000 | -0.8619 | 0.2030 | 0.7970 | 0.1160 | 0.8531 |
| 600 | 0.0039 | -0.8255 | 0.3522 | 0.6478 | 0.1848 | 0.8553 |
| 900 | 0.0039 | -0.8321 | 0.2881 | 0.7119 | 0.1861 | 0.6981 |

QA mix SFT 相比 grounding-only，在完整游戏合法动作率上有一定提升，尤其 600 步达到 `0.3522`。但胜率几乎没有起来，说明该数据配方仍不足以让模型学会稳定端到端策略。900 步 grounding target match 明显下降到 `0.6981`，完整游戏合法动作率也下降，提示该配方继续训练可能损伤基础 grounding / 执行能力。

这组结果适合放在附录或演进分析里，用来说明为什么最终不选 QA mix 作为主线。

### 4.3 End-to-end mix SFT：策略能力随训练逐步形成

| checkpoint | 胜率 | 奖励 | 规则合法动作率 | fallback | 自主出牌推进 | grounding target match |
|---|---:|---:|---:|---:|---:|---:|
| 100 | 0.0039 | -0.8141 | 0.4709 | 0.5291 | 0.2572 | 0.6693 |
| 200 | 0.0078 | -0.7965 | 0.4905 | 0.5095 | 0.2846 | 0.8290 |
| 300 | 0.0156 | -0.7720 | 0.5185 | 0.4815 | 0.3314 | 0.9033 |
| 400 | 0.0352 | -0.6864 | 0.6206 | 0.3794 | 0.4486 | 0.8873 |
| 500 | 0.0273 | -0.7041 | 0.6491 | 0.3510 | 0.4100 | 0.9562 |
| 600 | 0.0469 | -0.6602 | 0.6830 | 0.3170 | 0.4223 | 0.9515 |

End-to-end mix SFT 呈现出最清晰的 SFT 学习曲线。胜率整体从接近 0 增至 `0.0469`；奖励从 `-0.8141` 到 `-0.6602`；规则合法动作率从 `0.4709` 到 `0.6830`；fallback 从 `0.5291` 降到 `0.3170`。

500 步的 grounding target match 最高，为 `0.9562`，但完整游戏胜率和奖励不如 600 步。因此如果报告重点是端到端打牌，应选 600 步；如果单独展示 grounding 技术，可提 500 步是 grounding 近峰值，但不是最终主模型。

### 4.4 GRPO：前中期快速提升，后期出现质量回落

| checkpoint | 胜率 | 奖励 | 规则合法动作率 | fallback | 自主出牌推进 | grounding target match |
|---|---:|---:|---:|---:|---:|---:|
| 30 | 0.0820 | -0.5320 | 0.7747 | 0.2253 | 0.6455 | 0.8930 |
| 45 | 0.1523 | -0.3912 | 0.7871 | 0.2129 | 0.6398 | 0.9181 |
| 60 | 0.1758 | -0.3305 | 0.8042 | 0.1958 | 0.6977 | 0.8784 |
| 75 | 0.1641 | -0.3572 | 0.8021 | 0.1979 | 0.6850 | 0.8487 |
| 90 | 0.1797 | -0.3426 | 0.7821 | 0.2179 | 0.6152 | 0.8743 |
| 105 | 0.1641 | -0.3990 | 0.6804 | 0.3196 | 0.5619 | 0.8381 |

GRPO 的前 60 步是主要收益区间。step 30 已经超过 Kimi raw 的部分动作质量指标，并明显超过 SFT 胜率；step 45 和 step 60 继续提升胜率与奖励。step 60 是综合最优点，因为它同时拥有最高奖励、最高规则合法动作率、最低 fallback 和最高自主出牌推进。

step 90 虽然胜率最高，但只比 step 60 多 1 局胜场，且奖励、规则合法动作率、fallback 和自主出牌推进均不如 step 60。因此不适合作为主 checkpoint。step 105 则更适合作为“RL 后期质量回落”的展示点：它不是为了展示最好结果，而是展示继续训练的风险和多指标选择 checkpoint 的必要性。

## 五、推荐展示方式

主文建议放三张表：

1. 七个核心结果完整游戏表：展示胜率、奖励、合法动作率、fallback、自主出牌推进。
2. 七个核心结果的 grounding / 执行能力表：其中 Kimi raw 标注为 N/A，因为没有同协议 grounding 评测。
3. GRPO checkpoint 演进表：突出 step 60 是综合最优，step 105 是后期退化。

报告叙事建议按下面顺序展开：

1. 原始模型 baseline 几乎不会玩，9B 也不能自然解决问题。
2. Kimi K2.6 raw 提供可用但有噪声的教师行为。
3. Grounding-only SFT 证明模型能学会 GUI 执行，但端到端策略仍缺失。
4. End-to-end mix SFT 让模型开始形成完整游戏策略。
5. GRPO step 60 显著提升综合表现，并超过 Kimi raw 参考水平。
6. GRPO step 105 说明 RL 后期可能退化，需要基于验证指标选择 checkpoint。

最终主结果建议写为：

> 在 256 局在线斗地主评测中，GRPO step 60 将 Qwen3.5-4B baseline 的胜率从 0 提升到 17.58%，将规则合法动作率从 5.28% 提升到 80.42%，将 fallback 依赖从 94.72% 降到 19.58%，并将模型自主出牌推进率从 1.21% 提升到 69.77%。相较于 Kimi K2.6 raw 教师轨迹统计，GRPO step 60 也表现出更高胜率、更高合法动作率和更低 fallback，说明该训练流程不只是复现教师行为，而是在端到端环境反馈下获得了更强的游戏执行与决策能力。

同时建议加一句边界说明：

> Kimi K2.6 raw 是离线合成轨迹统计，不是同 seed 在线评测结果，因此主要作为教师数据源质量参考，而非严格 leaderboard 对照。
